<!DOCTYPE html>
<html lang="en">
    <%- include('../includes/head') %>

    <body>
        <%- include('../includes/header') %>

        <main>
            <h1>User Details</h1>  
            <% if(isUserIdValid) { %>   
                <% if(users.length < 1) { %>   
                    <p>No users found</p>
                <% } else if(userId >= users.length) { %>
                        <p>User id <%= userId %> does not exist.</p>
                <% } else { %>
                        <div>
                            <p class="spacing"><strong>Firstname: </strong> <%= users[userId].firstname %></p>
                            <p class="spacing"><strong>Lastname: </strong> <%= users[userId].lastname %></p>
                            <p class="spacing"><strong>Email: </strong> <%= users[userId].email %></p>
                            <p class="spacing"><strong>Password: </strong> <%= users[userId].password %> </p>
                        </div>                                
                    <%  } %>
            <% } else { %>
                <p>Please enter a number for the user id.</p>
            <% } %>
        </main>
        
        <%- include('../includes/footer') %>

    </body>

</html>